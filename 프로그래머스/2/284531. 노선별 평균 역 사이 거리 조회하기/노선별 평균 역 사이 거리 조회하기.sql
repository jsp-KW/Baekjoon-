-- 코드를 작성해주세요

# SELECT ROUTE, CONCAT(ROUND(SUM(D_BETWEEN_DIST),1), 'km') AS TOTAL_DISTANCE, CONCAT (ROUND(SUM(D_BETWEEN_DIST) / COUNT(*),2),'km') AS AVERAGE_DISTANCE
# FROM SUBWAY_DISTANCE GROUP BY ROUTE ORDER BY ROUND(SUM(D_BETWEEN_DIST),1) DESC;








# CONCAT 으로 문자열을 만들어서 ORDER BY에 그냥 정렬하면 바로 문자순 정렬임...
SELECT ROUTE , CONCAT(ROUND(SUM(D_BETWEEN_DIST),1),"km") AS TOTAL_DISTANCE, CONCAT(ROUND(AVG(D_BETWEEN_DIST),2),"km") AS AVERAGE_DISTANCE
FROM SUBWAY_DISTANCE GROUP BY ROUTE ORDER BY SUM(D_BETWEEN_DIST) DESC;































